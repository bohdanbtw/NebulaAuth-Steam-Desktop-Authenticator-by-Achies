# Copilot Instructions

## Project Guidelines
- When adapting logic from the user's Android project to NebulaAuth desktop, use a similar strategy but implement a desktop-appropriate approach rather than direct parity.
- Prefer using the system's real browser instead of recreating full browser UI features (tabs/bookmarks) inside embedded WebView when possible.